version: 2
jobs:
  build:
    docker:
      - image: hashicorp/terraform
    steps:
      - checkout
      - run:
          command: terraform fmt -check -recursive -diff
      - run: terraform init
      - run: terraform validate